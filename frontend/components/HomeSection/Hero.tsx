import Bounded from "../Bounded";
import AnimatedContent from "./AnimatedContent";

const Hero = () => {
  return (
    <Bounded>
      <AnimatedContent />
    </Bounded>
  );
};

export default Hero;
